# ──────────────────────────────────────────────────────────────────────────
#  Workday RAAS (Report-as-a-Service) — server-side configuration
#
#  The app reads the report URL and ISU credentials from here (server side):
#  the "Fetch User Activity" button sends an EMPTY request body and the API
#  route fills in these values. They are read ONLY in the Next.js API route,
#  are never sent to the browser, and are never logged.
# ──────────────────────────────────────────────────────────────────────────

# Full Workday RAAS report URL (https). Example:
#   https://wd2-impl-services1.workday.com/ccx/service/customreport2/<tenant>/<owner>/View_User_Activity_-_MG
# A ?format=json suffix is added automatically if absent.
#
# The From_Moment / To_Moment date window is set dynamically from the From/To
# date pickers in the UI (From = 00:00:00.000, To = 23:59:59.999). Any moments
# left in this URL are overwritten on each fetch — so you can omit them.
RAAS_URL=

# ISU (Integration System User) Basic-auth credentials.
RAAS_USERNAME=
RAAS_PASSWORD=

# Optional: timezone offset for the From/To moments (e.g. "-07:00", "+05:30",
# "Z"). Used only when the RAAS_URL has no existing moment to copy the offset
# from. Defaults to -07:00.
RAAS_TZ_OFFSET=

# ── Integration → ISU attachment check ───────────────────────────────────
# A second RAAS report that, prompted with an integration name, returns the
# ISU it is tagged to (Workday_Account). The app prompts it with each CLAR's
# integration name (the file name without .clar) during Analyse and uses the
# result to auto-score the ISU-Ready criterion.
#
# Put the integration name into the URL one of two ways:
#   1. (recommended) include the literal token {integration} where the prompt
#      value goes — it is URL-encoded and substituted per CLAR. e.g.
#      https://<tenant>/ccx/service/customreport2/<t>/<o>/Integration_ISU?Integration_System_Name={integration}
#   2. or set RAAS_INTEGRATION_PARAM to the prompt's query-parameter name and
#      leave the value off; the app appends it.
# Leave RAAS_INTEGRATION_URL unset to disable the check.
RAAS_INTEGRATION_URL=
RAAS_INTEGRATION_PARAM=

# Optional: a separate ISU for the integration report. If blank, the shared
# RAAS_USERNAME / RAAS_PASSWORD are used.
RAAS_INTEGRATION_USERNAME=
RAAS_INTEGRATION_PASSWORD=

# Optional: lock the RAAS endpoint to a fixed URL (defense-in-depth — prevents
# the route from being used as an open proxy). If set, the UI-supplied URL must
# start with this value. Leave blank to allow any https Workday URL.
RAAS_ALLOWED_URL_PREFIX=
